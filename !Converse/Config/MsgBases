;
; Messagebase Global Configuration
;

; Default retention for all areas (days, 0 = forever)
default_retention   365

; Default access level for new bases
default_accesslevel 0

; Path to message storage root
storage_root        <Converse$Dir>.MsgBases

; Default origins - if more than one is present then randomly chosen
origin `The Yellow Toaster BBS`
origin `Your friendly neighbourhood BBS`
origin `Retro computing since 2024`

; Include individual messagebase definitions
include <Converse$Dir>.Config.MsgBase_1
