;
; Filebase Global Configuration
;

; Default access level for new bases
default_accesslevel 0

; Path to file storage root
storage_root        <Converse$Dir>.FileBases

; Maximum file size for uploads (bytes, 0 = unlimited)
max_upload_size     0

; Include individual filebase definitions
include <Converse$Dir>.Config.FileBase_1
