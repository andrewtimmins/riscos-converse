{
    cls

    doing `Welcome...`
    type `<Converse$Dir>.BBS.Welcome`
    
    if %{registered} == 1 then
        doing `Registration reminder...`
        type  `<Converse$Dir>.BBS.Register`
    end if

    doing `Today in history...`
    oscli `Filer_Run <Converse$Dir>.BBS.Utils.!Today`
    sleep 500
    type `<Converse$Dir>.BBS.Today`

    cls
    doing `Login scan...`
    /* loginscan */
    anykey


}
