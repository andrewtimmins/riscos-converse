{
 report `Leaving NextCaller message`
 attr 0 6
 cls
}
 Loading `{fg 7}NextCaller.Write{fg 6}`
{
 runtask `<RiscBBS$Dir>.SubTasks.!NextCall.Write -door HANDLE ANSI USER`
 wait
 show `<RiscBBS$Dir>.BBS.AnyKey`
 attr 0 6
 cls
 eof
}
